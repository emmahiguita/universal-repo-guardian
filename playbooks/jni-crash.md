# JNI Crash Playbook
Find first native/JNI error → validate signature/ABI/thread attachment/ref ownership → inspect pending exceptions/pointers → minimal fix → symbol/build validation → device runtime → repeated lifecycle.
