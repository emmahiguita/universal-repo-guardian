# Dependency Conflict Playbook
Extract dependency graph → isolate conflicting versions/API/ABI → verify authoritative compatibility → pin minimal compatible change → build → targeted runtime → regression.
